@model  SCRP.Web.Models.Posts.PostListViewModel
@{
    ViewBag.Title = "Detail";
}


<!-- popular_causes_area_start  -->
<div class="popular_causes_area pt-120">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-9">
                <div class=" text-center mb-55">
                    <h3><span style="font-size:21px; padding-bottom:7px;">Lost Pet Register</span></h3>
                    <p>Missing Pet Register</p>
                </div>
            </div>
        </div>

        @*<div class="causes_content listOfRegisterBenefitsMK">
            <div class="sel sel--black-panther">
                <select name="select-profession" id="select-profession">
                    <option value="" disabled>All Breeds</option>
                    <option value="">Breed1</option>
                    <option value="">Breed2</option>
                    <option value="">Breed3</option>
                    <option value="">Breed4</option>
                    <option value="">Breed5</option>
                </select>
            </div>

            <div class="sel sel--black-panther">
                <select name="select-profession" id="select-profession">
                    <option value="" disabled>All Counties</option>
                    <option value="">County1</option>
                    <option value="">County2</option>
                    <option value="">County3</option>
                    <option value="">County4</option>
                    <option value="">County5</option>
                </select>
            </div>
            <a href="#" class="genric-btn danger radius searchBtnLostPet">Search</a>
        </div>*@


        <div class="row">
            @{foreach (var post in Model.Posts)
                {
                    <div class="col-lg-4 col-md-6">
                        <div class="single_cause">
                            <div class="thumb max">
                                <img class="imageOpacityMK" src="@Url.Content("~/images/" + post.ImagePostMappings[0].Image.Path)" alt="">
                            </div>
                            <div class="causes_content">
                                <div class="balance justify-content-between align-items-center textCenterMK">
                                    <br>
                                    <span class="span2">PET ID: #@post.Id</span>
                                    <br>
                                    <span class="span1">
                                        @post.Title
                                        @post.District
                                    </span>
                                </div>
                                <p>Disappeared on @post.MissedDate</p>
                                <a class="read_more" href="@Url.Action("PetDetail", "Advertisement",new { id = post.Id })">View Detail</a>
                            </div>
                        </div>
                    </div>
                }
            }
        </div>
    </div>
    <!-- popular_causes_area_end  -->
